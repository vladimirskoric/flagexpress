package ph.devcon.flag.entrypoint.api.rest.auth;

// disabled for now. quarkus test does end to end and the mocking is not that good.. will replace with mockito or
// or something.

//@QuarkusTest
public class AuthResourceTest {


//    @Test
    public void login() {

//        given()
//          .when().post("/authlogin", new LoginRequest())
//          .then()
//             .statusCode(200)
//             .body(notNullValue(LoginResponse.class));
    }

}